{
  "name": "whatsapp-birthday-bot",
  "version": "1.0.0",
  "description": "WhatsApp bot for sending scheduled birthday messages using Baileys",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon --ignore auth_info_baileys --ignore src/data --ignore src/backups src/app.js",
    "test": "jest"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.0",
    "@whiskeysockets/baileys": "^7.0.0-rc.6",
    "dotenv": "^16.0.0",
    "node-cron": "^3.0.0",
    "pino": "^8.0.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^2.0.0",
    "pino-pretty": "^13.0.0"
  }
}
